<div class="container">
	<div class="row">

		<h1>All Pictures</h1>
		
		<% @pictures.each do |pic| %>
			<h3>Link: <%= pic.link %></h3>
			<%= image_tag(pic.link, height: 200) %><br><br>
		<% end %>
		
	</div>
</div>